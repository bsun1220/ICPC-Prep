#include <set>
#include <iostream>
#include <vector>
#include <map>
#include <deque>
#include <stack>
#include <queue>
#include <algorithm>
#define ll                    long long int
using namespace std;


int main(){
    int n; cin >> n;
    int arr[n+1];
    for(int i = 1; i <= n; i++){
        cin >> arr[i];
    }
    int r1; cin >> r1;
    int c1; cin >> c1;
    int r2; cin >> r2;
    int c2; cin >> c2;
    ll num_steps = 0;
    queue<int> q; 
    cout << num_steps << endl;
}